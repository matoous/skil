# skil docs

This directory contains the Astro + Starlight documentation site for `skil`.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The static output is written to `docs/dist` and deployed via the root `wrangler.toml` configuration.

---
title: completions
description: Generate shell completion scripts.
---

```bash
skil completions <shell>
```

Supported shells include `bash`, `zsh`, `fish`, `elvish`, and `powershell`.

## Example

```bash
skil completions zsh
```
